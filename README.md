# Simple-Automation-Using-Ansible
INn this project we deploy 5 vagrant machines with ansible installed in one node to manage the other for nodes , basic servers with  mariadb, nginx, pache and postgresql
